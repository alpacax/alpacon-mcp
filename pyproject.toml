[project]
name = "alpacon-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.4",
    "httpx>=0.25.0",
    "websockets>=15.0.1",
]
